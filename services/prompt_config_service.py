"""Сервис для управления промптами AI"""
import json
import logging
from pathlib import Path
from typing import Dict, Optional

logger = logging.getLogger(__name__)

PROMPTS_CONFIG_FILE = Path("config/prompts.json")


class PromptConfigService:
    """Сервис для управления промптами AI"""
    
    def __init__(self):
        self.prompts_config_file = PROMPTS_CONFIG_FILE
        self.prompts_config_file.parent.mkdir(parents=True, exist_ok=True)
        self.prompts: Dict[str, Dict[str, str]] = {}
        self._load_prompts()
    
    def _load_prompts(self):
        """Загружает промпты из файла"""
        try:
            if self.prompts_config_file.exists():
                with open(self.prompts_config_file, 'r', encoding='utf-8') as f:
                    self.prompts = json.load(f)
                logger.info(f"Загружено {len(self.prompts)} промптов")
            else:
                # Создаем файл с дефолтными промптами если его нет
                self._create_default_prompts()
                logger.info("Создан файл промптов с дефолтными значениями")
        except Exception as e:
            logger.error(f"Ошибка при загрузке промптов: {e}")
            self._create_default_prompts()
    
    def _create_default_prompts(self):
        """Создает файл с дефолтными промптами"""
        default_prompts = {
            "generate_post": {
                "name": "Генерация поста",
                "description": "Системный промпт для генерации обычных постов",
                "system_prompt": "Ты профессиональный копирайтер для строительной компании \"Археон\".\nТвоя задача - создавать короткие, яркие и информативные посты для социальных сетей.\n\nКРИТИЧЕСКИ ВАЖНЫЕ ТРЕБОВАНИЯ:\n**СТРОГОЕ ОГРАНИЧЕНИЕ ДЛИНЫ: Текст поста НЕ ДОЛЖЕН превышать 900 символов (включая пробелы и эмодзи). Это абсолютное ограничение. Посты длиннее 900 символов будут отклонены системой.**\n- Используй много эмодзи для визуального оформления (минимум 1 эмодзи на каждые 2-3 предложения)\n- Посты должны быть КОРОТКИМИ и ёмкими (максимум 120-150 слов)\n- Структурируй текст короткими абзацами (по 2-3 предложения)\n- Используй эмодзи для выделения ключевых моментов: 📊 📸 🏗️ ✅ ⚠️ 💡 📝 и другие\n- Стиль: дружелюбный, современный, с эмодзи для привлечения внимания\n- Избегай длинных предложений и сложных конструкций\n- НЕ добавляй комментарии о соответствии требованиям или мета-описания\n- Возвращай ТОЛЬКО текст поста, без дополнительных комментариев\n- ПЕРЕД ОТПРАВКОЙ проверь длину текста - он должен быть строго до 900 символов"
            },
            "refine_post": {
                "name": "Редактирование поста",
                "description": "Системный промпт для переработки поста с учетом правок",
                "system_prompt": "Ты профессиональный редактор текстов для строительной компании \"Археон\".\nТвоя задача - переработать пост с учетом правок руководителя.\n\nВАЖНЫЕ ТРЕБОВАНИЯ:\n**СТРОГОЕ ОГРАНИЧЕНИЕ ДЛИНЫ: Текст поста НЕ ДОЛЖЕН превышать 900 символов (включая пробелы и эмодзи). Это абсолютное ограничение. Посты длиннее 900 символов будут отклонены системой.**\n- Используй много эмодзи для визуального оформления (минимум 1 эмодзи на каждые 2-3 предложения)\n- Посты должны быть КОРОТКИМИ и ёмкими (максимум 120-150 слов)\n- Структурируй текст короткими абзацами (по 2-3 предложения)\n- Используй эмодзи для выделения ключевых моментов: 📊 📸 🏗️ ✅ ⚠️ 💡 📝 и другие\n- Стиль: дружелюбный, современный, с эмодзи для привлечения внимания\n- Избегай длинных предложений и сложных конструкций\n- НЕ добавляй комментарии о соответствии требованиям или мета-описания\n- Возвращай ТОЛЬКО текст поста, без дополнительных комментариев\n- ПЕРЕД ОТПРАВКОЙ проверь длину текста - он должен быть строго до 900 символов"
            },
            "generate_from_sources": {
                "name": "Генерация из источников",
                "description": "Системный промпт для генерации поста на основе анализа других источников",
                "system_prompt": "Ты профессиональный копирайтер для строительной компании \"Археон\".\nТвоя задача - проанализировать посты из других источников (конкурентов, партнеров, отраслевых каналов) и создать НОВЫЙ, УНИКАЛЬНЫЙ пост для компании \"Археон\".\n\nКРИТИЧЕСКИ ВАЖНЫЕ ТРЕБОВАНИЯ:\n**СТРОГОЕ ОГРАНИЧЕНИЕ ДЛИНЫ: Текст поста НЕ ДОЛЖЕН превышать 900 символов (включая пробелы и эмодзи). Это абсолютное ограничение. Посты длиннее 900 символов будут отклонены системой.**\n- Используй много эмодзи для визуального оформления (минимум 1 эмодзи на каждые 2-3 предложения)\n- Посты должны быть КОРОТКИМИ и ёмкими (максимум 120-150 слов)\n- Структурируй текст короткими абзацами (по 2-3 предложения)\n- Используй эмодзи для выделения ключевых моментов: 📊 📸 🏗️ ✅ ⚠️ 💡 📝 🔗 и другие\n- Стиль: дружелюбный, современный, с эмодзи для привлечения внимания\n- Избегай длинных предложений и сложных конструкций\n- НЕ добавляй комментарии о соответствии требованиям или мета-описания\n- Возвращай ТОЛЬКО текст поста, без дополнительных комментариев\n- ПЕРЕД ОТПРАВКОЙ проверь длину текста - он должен быть строго до 900 символов\n\nЗАДАЧА:\n1. Проанализируй предоставленные посты из других источников\n2. Определи ключевые темы, тренды и интересные идеи\n3. Создай НОВЫЙ, ОРИГИНАЛЬНЫЙ пост для компании \"Археон\", который:\n   - НЕ копирует текст напрямую из источников\n   - Использует идеи и темы, но перерабатывает их в уникальный контент\n   - Соответствует стилю и тематике строительной компании\n   - Будет интересен нашей аудитории\n   - Содержит полезную информацию или инсайты\n\nВАЖНО: Не копируй текст из источников! Используй их как вдохновение для создания собственного уникального контента."
            },
            "analyze_photo": {
                "name": "Анализ фотографии",
                "description": "Промпт для анализа фотографий со строительных объектов",
                "user_prompt": "Проанализируй эту фотографию со строительного объекта.\nОпиши что на ней изображено: тип работ, этап строительства, особенности участка,\nвидимые проблемы или сложности, используемые материалы и технологии.\nБудь конкретным и профессиональным."
            }
        }
        self.prompts = default_prompts
        self._save_prompts()
    
    def _save_prompts(self):
        """Сохраняет промпты в файл"""
        try:
            with open(self.prompts_config_file, 'w', encoding='utf-8') as f:
                json.dump(self.prompts, f, ensure_ascii=False, indent=2)
            logger.info(f"Сохранено {len(self.prompts)} промптов")
        except Exception as e:
            logger.error(f"Ошибка при сохранении промптов: {e}")
    
    def get_prompt(self, prompt_key: str, prompt_type: str = "system_prompt") -> Optional[str]:
        """
        Получает промпт по ключу
        
        Args:
            prompt_key: Ключ промпта (например, "generate_post", "refine_post")
            prompt_type: Тип промпта ("system_prompt" или "user_prompt")
            
        Returns:
            Текст промпта или None если не найден
        """
        if prompt_key in self.prompts:
            prompt_data = self.prompts[prompt_key]
            return prompt_data.get(prompt_type)
        return None
    
    def set_prompt(self, prompt_key: str, prompt_type: str, prompt_text: str):
        """
        Устанавливает промпт
        
        Args:
            prompt_key: Ключ промпта
            prompt_type: Тип промпта ("system_prompt" или "user_prompt")
            prompt_text: Текст промпта
        """
        if prompt_key not in self.prompts:
            self.prompts[prompt_key] = {
                "name": prompt_key,
                "description": "",
                prompt_type: prompt_text
            }
        else:
            self.prompts[prompt_key][prompt_type] = prompt_text
        
        self._save_prompts()
        logger.info(f"Промпт {prompt_key} ({prompt_type}) обновлен")
    
    def get_all_prompts(self) -> Dict[str, Dict[str, str]]:
        """Возвращает все промпты"""
        return self.prompts.copy()
    
    def get_prompt_info(self, prompt_key: str) -> Optional[Dict[str, str]]:
        """
        Получает информацию о промпте
        
        Args:
            prompt_key: Ключ промпта
            
        Returns:
            Словарь с информацией о промпте или None
        """
        return self.prompts.get(prompt_key)

