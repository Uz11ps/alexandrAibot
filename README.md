# AI Bot для компании «Архион»

Многофункциональный бот для автоматического управления контентом в ВК и Telegram с использованием AI.

## Возможности

- 🤖 Автоматическая генерация постов с помощью AI
- 📅 Расписание публикаций по дням недели
- 📸 Анализ фотографий через AI
- ☁️ Интеграция с Google Drive
- 🔄 Ротация прокси и API ключей
- 📊 Администраторская панель в Telegram

## Технологии

- Python 3.11+
- aiogram 3.x (Telegram Bot API)
- OpenAI API
- Google Drive API
- APScheduler (планировщик задач)
- Docker

## Установка и запуск

### Локальная разработка

1. Клонируйте репозиторий:
```bash
git clone <URL_РЕПОЗИТОРИЯ>
cd AlexandrProfiStroi
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
# Отредактируйте .env и заполните все необходимые переменные
```

5. Запустите бота:
```bash
python main.py
```

### Docker

1. Соберите образ:
```bash
docker build -t alexandr-profi-bot .
```

2. Запустите контейнер:
```bash
docker-compose up -d
```

3. Просмотр логов:
```bash
docker-compose logs -f
```

## Конфигурация

Все настройки находятся в файле `.env`. См. `SETUP_INSTRUCTIONS.md` для подробной информации.

### Основные переменные:

- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `TELEGRAM_CHANNEL_ID` - ID канала для публикаций
- `OPENAI_API_KEY` - основной API ключ OpenAI
- `OPENAI_API_KEYS` - дополнительные ключи (через запятую)
- `OPENAI_PROXY_ENABLED` - включить/выключить прокси
- `OPENAI_PROXY_URL` - список прокси (через запятую)
- `GOOGLE_DRIVE_ENABLED` - включить/выключить Google Drive

## Деплой на сервер

См. `DEPLOY.md` для подробных инструкций по деплою.

Быстрый старт:
```bash
chmod +x deploy.sh
./deploy.sh
```

## Структура проекта

```
.
├── config/           # Конфигурационные файлы
├── handlers/         # Обработчики сообщений
├── services/         # Бизнес-логика
├── data/             # Данные (сотрудники, настройки)
├── storage/          # Хранилище файлов
├── credentials/      # Учетные данные (не в Git)
├── scripts/          # Вспомогательные скрипты
├── main.py           # Точка входа
├── Dockerfile        # Docker образ
├── docker-compose.yml # Docker Compose конфигурация
└── requirements.txt  # Зависимости Python
```

## Документация

- `SETUP_INSTRUCTIONS.md` - Инструкции по настройке
- `GOOGLE_DRIVE_SETUP.md` - Настройка Google Drive
- `DEPLOY.md` - Инструкции по деплою

## Лицензия

Приватный проект для компании «Архион»
